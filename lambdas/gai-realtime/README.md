# gai-realtime (future)

Placeholder for a real-time GAI streaming Lambda.

Planned features:
- Server-sent events (SSE) or WebSocket support for live GAI updates
- Used by the live GAI panel in ATLAS Mission Control
- Partner site embeds that need push updates without polling

Create `index.js` here when ready to implement.
